version: '3.7'

services:
  app:
    container_name: todo
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    volumes:
      - ./:/app
      - /app/node_modules
    #environment:
    #- CHOKIDAR_USEPOLLING=true
